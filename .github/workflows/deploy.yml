name: Deploy to Server

on:
  push:
    branches: [master]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host:     ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key:      ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            set -e
            cd /home/ubuntu/botbot2

            echo "→ Pull latest code..."
            git pull origin master

            echo "→ Install/update dependencies..."
            venv/bin/pip install -r requirements.txt -q

            echo "→ Restart app service..."
            sudo systemctl restart botbot2

            echo "→ Reload nginx (jika config berubah)..."
            sudo cp /home/ubuntu/botbot2/nginx.conf /etc/nginx/sites-available/botbot2
            sudo nginx -t && sudo systemctl reload nginx

            echo "✓ Deployed at $(date '+%Y-%m-%d %H:%M:%S')"
            sudo systemctl status botbot2 --no-pager -l
