# Freqtrade native install (Python 3.12).
# TA-Lib C library required first:  brew install ta-lib   (macOS)
#                                   apt install ta-lib     (Ubuntu, via ppa/source)
freqtrade==2026.6
scipy
scikit-learn
